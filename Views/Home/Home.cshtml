

<div class="mt-3 mb-5">
    <div class="container-fluid text-center">
        @if (!this.User.IsInRole("NotActive"))
        {
            <h2>Greetings, @this.User.Identity.Name!</h2>
            <h4>Enjoy your work today!</h4>
        }
        else
        {
            <h2>Greetings, @this.User.Identity.Name!</h2>
            <h4>You are not an Active User</h4>
        }
    </div>
</div>
